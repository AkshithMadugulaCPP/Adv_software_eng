package Assignment7.Q1;

public class Enums {
    // Enum for font
    public enum Font {
        ARIAL, CALIBRI, VERDANA
    }

    // Enum for color
    public enum Color {
        RED, BLUE, BLACK
    }

    // Enum for size
    public enum Size {
        SIZE_12, SIZE_14, SIZE_16
    }
}

